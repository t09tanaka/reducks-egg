export const ROOT_REDUCER_TEMPLATE = `import { combineReducers } from 'redux';

export const whitelist = [];
export const blacklist = [];

const reducers = combineReducers({});

export default reducers;
`
